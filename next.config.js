/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your existing config options remain exactly the same here
  // You can still use all the same configuration options
}

module.exports = nextConfig